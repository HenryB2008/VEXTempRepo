#include "goalRushBlue.h"

void goalRushBlue() {
    /*
    allianceColor = 1; 
    colorSortOn = false;
    chassis.setPose(50.138, -31.475, -105);
    chassis.moveToPose(13.068, -39.8, -105, 1300, {.lead = 0.03, .earlyExitRange = 2.5, }); //angle 105  (-14, -42)
    chassis.waitUntil(5);
    rightDoinker.extend(); 
    chassis.waitUntil(10);
    intakeMotor1.move(-127);
    intakeMotor1.set_brake_mode(pros::E_MOTOR_BRAKE_HOLD);
    chassis.waitUntil(29);
    intakeMotor1.brake(); 
    chassis.waitUntil(36.6); 
    gRushClamp.toggle(); 
    chassis.waitUntilDone(); 
    
    chassis.moveToPoint(29, -38, 700, {.forwards = false}, false); //angle 104 
    rightDoinker.toggle();
    gRushClamp.toggle(); 
    pros::delay(185); 

    chassis.turnToHeading(-192, 800, {}, false); //angle: 192
    chassis.moveToPoint(25, -21, 825, {.forwards = false}, false);
    mogo.toggle(); 
    intakeMotor1.set_brake_mode(pros::E_MOTOR_BRAKE_COAST); 
    colorSortOn = true; 
    pros::delay(1000);
    mogo.toggle(); 
    chassis.lateralPID.kI = 0.025; 
    chassis.moveToPoint(26, -28, 600, {}, false);
    chassis.lateralPID.kI = 0; 
    chassis.turnToPoint(13, -45, 800, {.forwards = false}, false);
    chassis.moveToPoint(13, -45, 1250, {.forwards = false}, false);
    mogo.toggle();
    //-15, -47,
    chassis.moveToPose(57, -55, -217, 3000, {.lead = 0.68}, false);
    chassis.tank(88, 88);
    pros::delay(800);
    chassis.tank(0,0); 

    pros::delay(500); 
    

    chassis.moveToPoint(chassis.getPose().x - (12 * sin(chassis.getPose(true).theta)), chassis.getPose().y - (12 * cos(chassis.getPose(true).theta)), 800., {.forwards = false}, false);     

  
    */
}

